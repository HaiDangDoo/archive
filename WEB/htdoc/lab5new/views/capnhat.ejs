<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Cập nhật thông tin cá nhân</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <%- include('nav') %>
    <main>
        <fieldset>
            <legend><h1>Cập nhật thông tin cá nhân</h1></legend>
            
        <form action="/capnhat" method="POST">
                <table>
                    <tr><th> <label for="name">Họ tên:</label></th>
                    <td>
                        <input type="text" id="name" name="name" value="<%= user.name %>" required>
    </td>         </tr>
             <tr><th>                     <label for="year_old">Năm sinh:</label>
             </th><td><input type="number" id="year_old" name="year_old" value="<%= user.year_old %>" required></td>
     </tr>
     <tr><th> <label for="sex">Giới tính:</label>
     </th>
     <td><select id="sex" name="sex">
        <option value="male" <%= user.sex === 'male' ? 'selected' : '' %>>Nam</option>
        <option value="female" <%= user.sex === 'female' ? 'selected' : '' %>>Nữ</option>
    </select></td></tr>
    <tr><th>    <label for="email">Email (no change):</label>
    </th>
    <td>            <input type="email" id="email" value="<%= user.email %>" disabled>
    </td></tr>
                </table>
            <button class="submit" type="submit">Cập nhật</button>
        </form>
        </fieldset>
    </main>
</body>
</html>
